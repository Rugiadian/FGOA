[일시 / Timestamp]
- 요청 시각: 2026-09-24 18:00:28
- 완료 시각: 2026-09-24 18:06:00

[요청 내용 / User Request]
- 레퍼런스 갤러리
  -- 디바이스 해상도별로 분류해서 볼 수 있게 할것.

[결과 요약 / Summary of Results]
1. 레퍼런스 갤러리(`ui/reference_gallery_dialog.py`) 디바이스 해상도별 분류 기능 구현:
   - 상단 툴바에 '📐 해상도별 분류' 드롭다운(`combo_resolution`) 추가
   - 갤러리에 보관 중인 전체 이미지의 해상도(width×height)를 동적으로 스캔하여 해상도별 이미지 개수 자동 집계 및 분류 목록 생성 (예: 1600×900 (3개), 1920×1080 (1개))
   - 현재 타겟 게임 창의 해상도와 일치하는 항목을 감지하여 `[현재 타겟]` 태그 우선 배치
2. 필터링 및 시각적 안내 강화:
   - 해상도 선택 시 해당 디바이스 해상도를 가진 이미지만 필터링 표출 (기존 사용 중/미사용 필터와 복합 적용 지원)
   - 목록 좌측 상단에 선택된 해상도 배너(`lbl_res_banner`) 및 현재 타겟 창 해상도 일치 여부(초록색 일치 배지) 표출
   - 각 이미지 항목 제목에 `[1600×900]` 해상도 태그 상시 표기
   - 상세 패널에서 현재 선택된 타겟 창 해상도와 일치하는지(`[현재 타겟 해상도와 일치 ✅]`) 또는 불일치하는지(`⚠️ 해상도 다름`) 실시간 비교 안내
3. 검증 및 버전 갱신:
   - `tests/test_reference_gallery_resolution.py` 단위 테스트 작성 및 통과
   - `core/version.py` -> `260924.1805`로 갱신 완료

[영향 파일 목록 / Affected Files]
- ui/reference_gallery_dialog.py (해상도별 분류 콤보박스, 필터링, 타겟 해상도 비교 안내)
- core/version.py (버전 260924.1805 갱신)
- tests/test_reference_gallery_resolution.py (해상도별 분류 단위 테스트 추가)
- history/2026-09-24_180600_레퍼런스_갤러리_디바이스_해상도별_분류_기능_구현.txt
