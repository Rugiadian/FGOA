======================================================================
[FGOA 작업 이력 로그]
======================================================================

1. 일시 (Timestamp)
   - 요청 시각: 2026-09-25 20:07:00
   - 완료 시각: 2026-09-25 20:14:00

2. 사용자 요청 내용 (User Request)
   - "조작 시각화를 하면 클릭 액션이 안 들어가는 경우가 있음. 조작에 영향없게 바람."
   - "앱 화면 상단의 진행중인 시퀀스 출력은 하지말고 플레이바에 할 것."
   - "플레이바 재생 벝느 두가지가 같은 이름으로 되있음. 선택 노드 재개, 전역 재개 로 표기 변경."
   - "익스펙터에서 저장 안한거 저장 경고 할 때 저장하겠냐 창에서 선택되있는 버튼 시각적 표시. 지금은 저장하기에 포인터가 가있는데 그대로 유지하고 시각적 표시 할 것."

3. 작업 결과 요약 (Summary of Results)
   - 1. 조작 시각화 오버레이의 완전한 마우스 투과(Click-Through) 보장:
     * `ui/action_overlay.py`의 `ActionOverlayWindow`에 네이티브 Windows 윈도우 스타일(`WS_EX_TRANSPARENT | WS_EX_LAYERED | WS_EX_NOACTIVATE`)을 적용하여 마우스 입력이 창을 100% 관통하도록 구현.
     * `nativeEvent`에서 `WM_NCHITTEST` (0x0084) 메시지 수신 시 `HTTRANSPARENT` (-1)을 반환하여 Windows OS 차원에서 마우스 히트테스트가 오버레이에 걸리지 않고 하위 타깃 앱 창으로 직통 전달되도록 완벽 차단.
   - 2. 시퀀스 진행 출력 위치 개선:
     * 타깃 앱 화면 상단 중앙에 배너 형태로 뜨던 액션 진행 안내(`_draw_banner_visualization`)를 타깃 화면에서 제거하여 게임/앱 화면을 가리지 않도록 개선.
     * 진행 중인 시퀀스 액션 세부 정보(마우스 클릭/드래그 좌표, 대기 시간 카운트다운, 키 입력 내용 등)를 미니 플레이바(`PopupPlayBar`)의 상태 표시창(`lbl_status_detail`)에 실시간으로 선명하게 포맷팅하여 출력하도록 구현.
   - 3. 플레이바 재개 버튼 명칭 분리 및 기능 명확화:
     * 일시정지(`paused`) 상태 시 동일하게 "▶ 재개"로 표기되던 두 개의 버튼을 각각 "▶ 선택 노드 재개"와 "▶ 전역 재개"로 명확히 분리 표기.
     * "▶ 선택 노드 재개"(`btn_play`): 콤보박스나 목록에서 선택된 시나리오 노드부터 이어서 재개.
     * "▶ 전역 재개"(`btn_pause`): 기존 일시정지 지점에서 전체 오토 흐름을 그대로 이어서 재개.
   - 4. 인스펙터 미저장 경고 대화상자 선택 버튼 시각적 강조:
     * `ui/main_window.py`에서 다른 시나리오 선택 시 뜨는 미저장 경고 대화상자를 커스텀 스타일 QMessageBox로 고도화.
     * 기존처럼 기본 버튼(Default Button) 및 포커스는 '저장' 버튼에 유지하면서, 선명한 파란색 Primary 배경(#2563eb), 밝은 파란색 테두리(#60a5fa), 볼드 폰트, 포커스 링을 적용하여 어떤 버튼이 선택되어 있는지 한눈에 확인할 수 있도록 시각적 표시 강화.
   - 5. 버전 갱신 및 검증:
     * `core/version.py`: `__version__ = "260925.2013"`으로 갱신.
     * 전체 1,141개 단위 테스트 100% 정상 통과 검증 완료.

4. 영향 파일 목록 (Affected Files)
   - core/version.py (수정)
   - ui/action_overlay.py (수정)
   - ui/popup_play_bar.py (수정)
   - ui/main_window.py (수정)
   - tests/test_playbar_and_action_sequence.py (수정)
   - history/2026-09-25_201400_조작시각화_플레이바표기_인스펙터경고버튼강조.txt (신규 생성)

======================================================================
